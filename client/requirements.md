## Packages
framer-motion | Smooth animations for list items and page transitions
date-fns | Formatting dates nicely

## Notes
Authentication uses Replit Auth (via /api/login, /api/logout, /api/auth/user)
API requests should use `credentials: "include"`
Frontend should handle 401 Unauthorized by redirecting to login or showing login prompt
